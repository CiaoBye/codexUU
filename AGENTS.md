# CodexUU 协作规范

默认使用中文回复。修改本项目时必须遵循以下规则：

---

## 1. 架构与技术栈约束

1. **核心技术架构**：本项目采用 **Tauri 2 + Rust + React 19 + TypeScript + Tailwind CSS** 构建面向 Windows 的本地优先 AI 编程控制台。
   - **后端（Rust）**：位于 `src-tauri/src/`，负责底层数据源解析（Codex、Antigravity）、价格估算计算、快照聚合、系统托盘、全局快捷键与窗口管理。
   - **前端（React 19）**：位于 `src/`，负责仪表盘渲染、方案 C 额度罗盘、用量图表、四列任务看板、项目排行、Skill 与设置中心。
   - **历史代码隔离**：旧版 Python (PySide6) 历史代码已完整隔离在 `old/` 目录，禁止在根目录新增 Python 依赖或向 `old/` 写入新业务逻辑。

2. **工程与依赖规范**：
   - 避免引入无用的第三方依赖；Rust crate 与 npm package 必须保持最小化。
   - 前端包管理器统一使用 `pnpm`。
   - 代码风格遵循 `.editorconfig`（UTF-8, LF, 通用 2 空格, Rust 4 空格）。

---

## 2. 界面与交互设计规范

1. **单页仪表盘布局**：
   - 顶部为全局导航与渠道切换（Codex 官方 / Antigravity / 全部聚合），包含主题切换、刷新、设置控件。
   - 上半区常驻 **方案 C 额度罗盘**（7D 紫色外环、5H 蓝色内环，已用自底向左增长、剩余自底向右增长，点击圆心即时切换口径）与 **4 张 Token 指标卡片**（今日/本周/本月/累计，带未缓存/缓存/输出精细比例条）。
   - 下半区通过 **“今日任务 / 用量趋势 / 项目排行 / Skill”** 四个 Tab 原地切换，禁止拆分为独立页面或引入左侧导航栏。

2. **用量趋势规范**：
   - 采用方案 B 通栏范围条，日期按钮左对齐（每日/本周/本月/累计）。
   - 图表必须从 0 基线绘制并显示 `0` 标注，峰值清晰标识，支持 Token / API 等效价值 ($) 切换。
   - 连续自然日补零呈现，缺失日期按 0 绘制，不得用活跃日拼接。

3. **任务看板规范**：
   - 按项目维度聚合，分为 **进行中**、**待处理**、**定时任务**、**已完成** 四列，展示最新线程摘要、渠道、时间与线程总数。

4. **项目排行规范**：
   - 仅统计本机仍存在的真实项目目录，排除临时与已删除目录；
   - 支持一键导出 JSON、CSV、Markdown 格式；
   - 右侧包含活跃项目数、用量集中度（Top 1 / Top 3）与最近活跃概览。

5. **设计令牌与动效**：
   - 语义色：未缓存输入（`#4d9fff` 蓝）、缓存输入（`#8b75ff` 紫）、输出（`#eca64c` 橙）、品牌主色（`#2c9f9b` 青绿）。
   - 支持深色与浅色双主题，SVG 图表元素优先使用 CSS 变量（`var(--accent-brand)` 等）。
   - 主窗口标准客户区最小尺寸为 `1060×720`。

---

## 3. 数据口径与计算准确性

1. **同源一致性**：同一渠道（Codex / Antigravity / 全部）的概览卡片、趋势图与项目排行必须来自完全相同的底层数据快照，严禁页面间数值自相矛盾。
2. **时区一致性**：时间统计与展示必须统一使用配置的统计时区（默认本地时区/北京时间）；“本周”严格从周一 00:00 起算，“本月”严格按当月 1 日 00:00 起算。
3. **精准计价**：API 等效价值根据模型 ID 精确匹配官方公开输入、缓存与输出单价分别计算，未知或网关别名模型标明未计价。
4. **工具与 Skill 口径**：仅统计显式 `function_call` / `custom_tool_call` 或 Skill 加载事件，不按调用次数分摊 Token 或将纯文本提及算作使用。

---

## 4. 版本与变更管理规范

1. **单一版本源**：
   - 根目录 `VERSION` 为单一版本来源（当前 `1.0.2`）。
   - 版本号同步至 `package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json`、`README.md` 与 UI 徽章。
   - 发布 Git 标签统一使用 `v<版本号>`。

2. **更新日志维护**：
   - 每次完成代码更新后，必须在 `CHANGELOG.md` 记录本次更新的版本、改动点、影响范围和验证结果。
   - 历史版本日志完整保留在 `CHANGELOG.md` 中。

3. **验证与测试门禁**：
   - 前端测试：`pnpm test`（Vitest）必须全部通过。
   - 后端测试：`cargo test`（位于 `src-tauri`）必须全部通过且 0 代码警告。
   - 类型检查：`pnpm run typecheck` 必须 0 错误。
   - 重启校验：推荐使用 `powershell -ExecutionPolicy Bypass -File scripts/restart.ps1` 验证桌面运行。

---

## 5. 资源与索引

- **版本更新日志**：[`CHANGELOG.md`](file:///c:/Users/Ayuan/Documents/Vibe/CodexUU/CHANGELOG.md)
- **项目仓库与 Issue**：`CiaoBye/codexUU` (GitHub Issues)
