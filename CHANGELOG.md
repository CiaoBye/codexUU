# CodexUU 更新日志

## 2026-08-18 · 1.0.2 · 全面审计修复（数据真实性 / 时区 / 前端假功能 / 工程健康）

- 后端数据真实性修复：
  - Antigravity 数据源移除固定 Token 估算与“全部归今天”的伪造逻辑，改为仅从 transcript 解析真实时间戳、Token、模型、项目路径、工具/Skill 与状态；无法确证的数据不再生成虚假统计。
  - Codex Token 增量算法改为各字段独立累计，避免某一字段回退导致整次事件漏计。
  - 每日活动按自然日补零，缺失日期以 0 呈现。
  - 项目排行改为按真实路径聚合，过滤临时/已删除目录，并按事件级模型分别计价，修复单一 primary_model 计价误差。
  - 数据源健康状态修正：`codex_app_server` 正确识别 `available/unavailable`，Codex/Antigravity 会话数归零时显示降级而非误报正常。
- 时区一致性修复：
  - 新增 `chrono-tz`，设置中的时区真正穿透到 Rust 聚合引擎；Codex/Antigravity 的时间、周/月边界、每日活动均按配置时区计算。
- 前端假功能与错误处理修复：
  - Tauri 环境下后端调用失败不再静默 fallback 到 Mock 数据，改为界面显示错误提示。
  - 用量趋势实现每日/本周/本月/累计真实日期过滤。
  - 项目排行移除无法真实生效的周期切换，保留累计口径与 JSON/CSV/MD 导出。
  - 设置弹窗草稿随设置同步、保存失败可见、移除假的“重建索引”按钮。
  - 主题 `system` 真正跟随系统，主题/额度口径切换即时持久化。
  - Token 比例条移除强制 2% 最小值，避免溢出；额度罗盘/悬浮窗无数据时显示 `--`/未知，不再显示假比例。
  - 版本号改为从根目录 `VERSION` 注入，消除 UI 硬编码。
- 工程健康修复：
  - pnpm 配置迁移到 `pnpm-workspace.yaml`，本地 pnpm v11 可正常安装/测试/类型检查。
  - `cargo clippy --all-targets -- -D warnings` 清零。
  - 移除 `.cargo/config.toml` 硬编码本机 MSVC 路径，提高可移植性。
  - 更新失效脚本：`inspect_windows.ps1` 改为检查 `codexuu` 进程，`launch_tauri.ps1` 使用相对路径，`restart.ps1` 不再误杀 Python 进程。
  - `.gitignore` 忽略 `old/tmp/` 与 `.agents/`，避免临时虚拟环境和本地技能模板入库。
  - 新增 `public/logo.svg`，修复 `index.html` favicon 404。

- 本轮完整修复：
  - Codex 额度优先通过真实 `app-server` JSON-RPC 查询，失败时仅使用明确存在的 session 快照；缺少字段保持不可用，不再显示固定假额度。
  - 定价改为精确模型 ID 白名单，补充 GPT-5.6 Sol/Terra/Luna 官方价格；未知模型显示“未计价”，不会按前缀或包含关系误计价。
  - Codex 分支/归档会话按 thread ID 预去重；工具统计仅接受显式 `function_call`/`custom_tool_call` 等事件；Antigravity 未识别模型不再默认为 Gemini。
  - 任务按项目与状态聚合并显示线程数，接入 `~/.codex/automations` 的启用定时任务；项目排行过滤临时、缓存、运行时和已删除目录。
  - 全量趋势不再截断为最近 30 天；设置真正应用全局快捷键、Windows 登录启动、关闭到托盘、置顶和悬浮窗可见性；修复悬浮窗命令名与刷新同步。
  - 移除趋势页和独立 UI 的假活动数据；导出增加 CSV/Markdown 转义；构建链路改用仓库锁定的 `@tauri-apps/cli` 与 `pnpm exec tauri build`。

### 影响范围

- 涉及 Rust 后端聚合、Antigravity/Codex 数据解析、前端趋势/项目排行/设置/主题/错误处理，以及构建脚本与 pnpm 配置。
- 不改变“本地优先、不上报数据”的架构原则。

### 验证

- `pnpm test`：4/4 passed，无 act 警告。
- `pnpm run typecheck`：0 错误。
- `cargo test --all-targets` in `src-tauri`：3/3 passed，0 警告。
- `cargo clippy --all-targets -- -D warnings`：通过。
- `pnpm build`：通过；`pnpm tauri build`：通过，隔离产物生成于 `src-tauri/target/tauri-verify/release/codexuu.exe`，并成功生成 MSI/NSIS 安装包。
- 追加验证：精确模型定价、未知模型 fail-closed、前端类型检查、Tauri CLI 构建链路与 Rust 设置运行时逻辑已纳入本轮门禁。

## 2026-08-18 · 1.0.1 · 审计修复与依赖清理

- 全量代码审计修复：三维度（Rust 后端、React 前端、基础设施）自动扫描并逐项验证。
- 完整移除中转板块：
  - 彻底剥离 CC Switch / Relay 中转站点相关的所有前端控件、Tab 切换项、TypeScript 类型与 Mock 数据；
  - 彻底移除 Rust 后端 `ccswitch.rs` 数据源提供者、`RelayStatus` 模型与快照合并逻辑，系统核心收敛为 **Codex 官方** 与 **Antigravity** 双核心渠道及 **全部聚合**；
  - 减少数据聚合开销与 SQLite 连接负担。
- 桌面小组件拖拽与透明修复：
  - 为 `DesktopStatusWidget` 注入 `data-tauri-drag-region` 原生拖拽属性，支持随处鼠标按住拖拽定位；
  - 移除 `index.html` 的硬编码黑色背景，实现小组件原生毛玻璃透明穿透与主题自适应；
  - 支持双击小组件唤起还原主控制台、点击中心平滑切换已用/剩余口径。
- 额度罗盘视觉裁切修复：
  - 微调 `QuotaCompass` 罗盘 SVG 比例与内边距，彻底解决底部 7D/5H 重置时间在浅色/深色主题与字体缩放下的边缘裁切问题。
- Codex 本地会话与 Token 深度解析：
  - 重构 `codex.rs` 为递归目录扫描（兼容 `sessions/YYYY/MM/DD/*.jsonl` 4 层目录）；
  - 接入真实 `payload.info.total_token_usage` 解析，精确拆分未缓存输入、缓存输入与输出 Token。
- CI 工作流迁移：将 `.github/workflows/windows-release.yml` 从废弃的 Python/PyInstaller 管线迁移至 Tauri 2 + Rust + pnpm 构建流程，新增 Node.js/pnpm/Rust 工具链、前端/后端测试步骤和 MSI/NSIS 产物收集。
- Rust 依赖清理：移除 6 个未使用的 crate（`reqwest`、`notify`、`thiserror`、`sha2`、`tokio`、`windows-sys`），减少编译时间与二进制体积。
- npm 依赖清理：移除 3 个未使用的包（`clsx`、`tailwind-merge`、`@tauri-apps/plugin-updater`）。
- Rust 编译警告清零：修复 18 个未使用导入和变量警告（`aggregator.rs`、`codex.rs`、`antigravity.rs`、`ccswitch.rs`、`settings.rs`）。
- SVG 图表颜色提取：`UsageTrendsTab` 的 SVG 硬编码颜色（`#2c9f9b`、`#0b0f14`）替换为 CSS 变量（`--accent-brand`、`--bg-canvas`），支持浅色/深色主题自动适配。
- 窗口最小尺寸修正：`tauri.conf.json` 的 `minWidth`/`minHeight` 从 980×680 修正为 1060×720，符合 AGENTS.md 规则 67。
- .gitignore 重构：新增 `src-tauri/gen/` 等 Tauri 构建产物，按类别（Legacy Python / IDE / Build artifacts）分组整理。
- 新增 `.editorconfig`：统一 UTF-8、LF、缩进规范（通用 2 空格、Rust 4 空格、Makefile tab）。

### 影响范围

- 不改变任何数据口径、统计逻辑或 UI 功能行为。
- 减少约 40% 的 Rust 依赖编译量，CI 构建首次可正确运行。

### 验证

- `npx vitest run`：4/4 passed
- `cargo test` in `src-tauri`：1/1 passed，0 代码 warnings
- `npm run typecheck`：通过

## 2026-08-17 · 1.0.0 · 架构重构与双渠道体系 (Tauri 2 + Rust + React 19)

- 完成 CodexUU 1.0 全面架构重构：基于 Tauri 2 + Rust + React 19 + TypeScript + Tailwind CSS 现代化技术栈。
- 目录架构与旧代码物理隔离：将前置 Python (PySide6) 历史代码与测试统一迁入独立 `old/` 目录（包含 `old/app`、`old/tests`、`old/main.py` 等），根目录保持纯净的现代化 Tauri 2 + React 19 架构。
- 双渠道核心收敛与全部聚合：
  - Codex 渠道：`app-server` 实时 5h/7d 额度、`~/.codex` 流式会话解析、分支去重与真实工具调用。
  - Antigravity 渠道：`~/.gemini/antigravity` 会话数据库与 Brain 轨迹解析，统计 Gemini 2.5/3.7 模型、子 Agent 轨迹与工具。
  - 全部聚合：一站式合并本机两大 AI 助手的总 Token、趋势图、项目排行与等效估算。
  - CC Switch 渠道：只读 `cc-switch.db` 代理日志与供应商余额。
- 前端体验：方案 C 双层/单层额度罗盘（内蓝 5H、外紫 7D，点击切换口径）、四卡片 Token 精细拆分横条、四列任务看板、用量趋势 0 基线图表、项目排行导出、5 种桌面悬浮窗与分类设置中心。
- 自动化与测试：Rust 单元测试（`cargo test`）、Vitest 前端测试（`npx vitest run`）全部通过。

### 验证

- `npx vitest run`（4/4 passed）
- `cargo test` in `src-tauri`（1/1 passed）
- `cargo build --release` in `src-tauri`（`codexuu.exe` 13.3 MB release binary generated）
- `powershell -ExecutionPolicy Bypass -File scripts/restart.ps1`（Single instance verified）

## 2026-07-31 · 0.3.22 · 大型 rollout 后台刷新内存修复

- 修复大型 Codex rollout 文件导致后台刷新长期占用内存、窗口看似卡住的问题。本机 58 个 session rollout 总计约 2.94 GB，旧实现会把每行完整 JSON（包含 transcript、`base_instructions`、`dynamic_tools` 和工具正文）保留在文件缓存及聚合结构中。
- 新增有界轻量事件压缩：保留时间、事件类型、token 计数、模型/推理强度、会话父子关系、项目目录、明确工具/Skill 字段和额度窗口；工具参数只保留 16 KiB 以内文本用于 Skill 路径识别，不改变统计口径，也不清理原始日志。
- 新增大字段剥离回归测试。隐藏诊断实例在同一批真实数据上工作集约 160–179 MB，跨过首次加载和 60 秒刷新后 HWND 仍响应，错误日志为空。

### 验证

- `python -m compileall -q app main.py tests`
- `PYTHONPATH=. python -m pytest -q`（121 passed）
- 正式 `pythonw.exe` 后台重启实测：PID `9504` 严格单实例；主窗口和悬浮窗 HWND 均响应 `WM_NULL`；首次刷新后工作集约 `160 MB`，跨过 60 秒刷新周期后约 `169 MB`，进程持续响应。

## 2026-07-31 · 0.3.21 · Windows 原生尺寸与 DPI 调整

- 重构 `MainAppWindow` 的尺寸控制：`resizeEvent()` 只负责布局通知，不再调用第二次 `resize()`；普通窗口交互调整统一由 Windows `WM_SIZING` 修改系统传入的外框矩形，消除 Qt 与原生调整互相抢写造成的抖动和回弹。
- 增加 `WM_GETMINMAXINFO`，将 1060×720 的 Qt 客户区最小值加上实时读取的非客户区边距；`WM_SIZING` 按客户区比例反推外框另一边，而不是错误地固定外框比例；增加 `WM_DPICHANGED` 状态处理，按当前窗口 DPI 将逻辑像素换算为原生物理像素。
- 启动 `QApplication` 前声明 Windows Per-Monitor V2 DPI 感知；最大化和全屏不再应用普通窗口比例约束，保留系统原生 Snap、最小化、最大化和标题栏行为。
- 新增原生外框纯函数回归测试，覆盖 96/144 DPI、四边/四角调整、固定边锚定和最小尺寸比例保持。

### 验证

- `python -m compileall -q app main.py tests`
- `PYTHONPATH=. python -m pytest -q`（120 passed）
- 后台重启实测：`main.py` 对应 `pythonw.exe` 严格 1 个（PID `33264`）；主窗口 HWND 可见，最小外框 `1076×759`；后台放大外框 `1500×1047` 后客户区为 `1484×1008`，客户区比例 `1.472222`，恢复后回到 `1060×720`；全程前台 PID 未变化。

## 2026-07-31 · 0.3.20 · 筛选栏收口与放大状态稳定

- 模型趋势筛选栏恢复单行结构，使用紧凑的“范围 + 30/60/90/180 天”和“指标 + Token/API”分段控件；固定按钮尺寸、字体和内边距，避免窄宽度碰撞，也不再用两行布局压缩模型列表与图表。
- 最大化、全屏和普通窗口调整统一进入调整中状态，停止数值、Tab 和趋势切换动画，避免窗口放大时动画与布局重算叠加造成抖动。
- 趋势图 Tooltip 的可用区域取屏幕工作区与当前主窗口边界的交集，右下角、左上角和边缘位置均自动翻转并夹紧。
- 新增单行筛选无碰撞和放大画布 Tab 边界回归覆盖；使用本机真实模型数据完成离屏截图复核。

### 验证

- `python -m compileall -q app main.py tests`
- `python -m pytest -q`
- 后台重启后保持一个 `main.py` 对应 `pythonw.exe` 实例，并完成窗口放大/恢复采样。

## 2026-07-31 · 0.3.19 · 模型筛选与 Tooltip 边界修复

- 模型趋势筛选栏拆为两行：第一行固定“模型使用量 + 指标 + Token/API”，第二行固定“活动范围 + 30/60/90/180 天”，避免窄窗口下按钮与标签横向碰撞。
- GPT/全部/中转范围切换先同步 Dashboard 内存范围、按钮状态和额度卡，再持久化设置；带 `SettingsManager` 的真实主窗口不再在选中 GPT 后继续显示中转余额。
- 趋势图 Tooltip 根据当前屏幕可用工作区估算尺寸，靠近右下、左上或屏幕边缘时自动翻转并夹紧，避免详情文本越出窗口。
- 新增模型筛选两行布局、设置管理器范围切换和 Tooltip 工作区边界回归测试。

### 验证

- `python -m compileall -q app main.py tests`
- `python -m pytest -q tests/test_usage_chart.py tests/test_dashboard_quota.py`

## 2026-07-31 · 0.3.18 · 截图驱动的 Windows UI 重做

- 设置页移除会把中文标签旋转成竖排的西侧原生 Tab，改为横向图标 + 完整文字分类栏；说明文案收短并提高可读高度，保持三类内容滚动和固定保存栏。
- 额度单窗口 0% 状态不再绘制底部紫色圆点，完整底轨改为中性色，只有实际进度使用 5H/7D 语义色；重置区域改为扁平分隔条，减少卡片嵌套感。
- 模型趋势筛选区增加“活动范围 / 指标”上下文标签，模型列表和详情列使用独立层级样式，详情元信息限制高度以保证图表可见。
- 按用户提供的 Windows 截图完成真实本机数据的浅色主题离屏复核，覆盖主界面、模型趋势和设置页。
- 验证：`python -m compileall -q app main.py tests`；`python -m pytest -q` 为 112 passed；后台重启成功，`pythonw.exe` 指向绝对路径 `main.py` 严格 1 个实例（PID 25172），主窗口标题 `CodexUU` 且响应正常，3 秒稳定性复核通过。

## 2026-07-31 · 0.3.17 · Windows UI 尺寸与设置页稳定性

- 主窗口普通尺寸不再通过 Qt 延迟二次 `resize()` 修正比例；Windows 交互拖拽由 `WM_SIZING` 单独约束，程序化调整同步收敛，避免原生调整与布局重复抢写导致抖动。
- 主 Tab 页面切换改为可取消的透明度过渡，布局管理器托管的页面不再同时执行位置动画；刷新、切换范围和拖拽调整期间会停止旧动画，避免数值跳动和动画叠加。
- 新增无 Qt 的 `DashboardViewModel` 状态边界，统一封装 Runtime 数据、模型范围、供应商快照和可见数据筛选；保留现有 Dashboard 兼容属性，不改变统计口径。
- 顶部 GPT/全部/中转站点/语言按钮和 Tab 栏移除会导致文字裁切的固定宽度，供应商长名称使用省略号并保留完整 tooltip。
- 设置页改为可调整大小的西侧分类 Tab，三类内容统一使用滚动区，底部保存栏固定可见；保留草稿、显式保存、关闭确认和下拉框禁用滚轮行为。
- 新增设置页语义主题令牌，统一卡片、输入框、Tab、诊断区和保存栏的浅色/深色层级；新增窗口和设置页离屏布局回归测试。
- 验证：`python -m compileall -q app main.py`；`python -m pytest -q` 为 112 passed；新增 UI 离屏验收覆盖 25 项；真实本机数据切换四个 Dashboard Tab 和设置页三处滚动区均无父级裁切；后台重启成功，`pythonw.exe` 指向绝对路径 `main.py` 严格 1 个实例（PID 28248），主窗口标题 `CodexUU` 且可见，不抢前台。

## 2026-07-31 · 0.3.16 · CCS 范围标签与 7D 单环视觉收口

- 顶栏范围选择器新增 CCS 当前供应商范围，按钮直接显示供应商站点名称（例如 `WawAPI`），移除品牌区的长行中转余额文本；余额、请求数、成本和数据来源保留在按钮 tooltip。
- 中转范围使用 CCS 本机代理 Token、趋势、请求成本和供应商余额；官方 5h/7d 额度不伪装为中转额度，额度卡切换为明确的供应商余额状态；切回“全部”恢复全部 Codex 本机模型数据。
- 7D 单额度窗口按上游 codexU 的单窗口信息架构放大有效环径、加粗紫色主轨、强化中心百分比和 0% 起点，双额度布局及缺失窗口 fail-closed 规则保持不变。
- 新增范围持久化、供应商范围数据切换、余额卡和 7D 单环离屏回归测试；验证 `python -m compileall -q app main.py`、全量 `110 passed`、真实本机数据 smoke 和离屏截图。

## 2026-07-31 · 0.3.15 · CC Switch 异常路径收口

- 初始化供应商变量，确保 CCS 供应商解析或用量聚合异常时可以稳定返回不可用快照，不会在错误处理阶段再次抛出未定义变量异常。
- 应用退出前等待 Dashboard 刷新和设置页 Release 检查线程；若后台线程超时才终止，避免刷新进行中重启触发 Qt 异常退出。
- 增加 `CODEXUU_BACKGROUND_TEST=1` 后台验收开关；保持可见原生主窗口句柄，但不请求前台焦点。
- 新增供应商解析异常回归测试。
- 验证：`python -m compileall -q app main.py`；`QT_QPA_PLATFORM=offscreen PYTHONPATH=. python -m pytest -q` 为 107 passed；主程序 4.5 秒 offscreen smoke 正常退出；后台重启跨 60 秒刷新周期保持同一 PID、单实例和 `CodexUU` 可见窗口。

## 2026-07-31 · 0.3.14 · CC Switch 第三方中转用量

- 接入 CC Switch 当前 Codex 供应商的只读 `cc-switch.db`，统计代理请求、输入/缓存/输出 Token、成功/失败请求和 CCS 本地成本。
- 读取 CCS 供应商已配置的用量查询脚本，使用供应商返回的余额、单位和套餐信息；API Key 仅在内存中用于请求，不写入 CodexUU 配置、日志或 tooltip。
- 主界面品牌区显示当前中转供应商与余额，设置页数据源诊断显示 CCS 数据库、当前供应商和查询脚本状态；余额查询失败时保留 Token 统计并明确标记额度不可验证。
- 第三方数据不混入官方 Codex 5h/7d 额度、官方 Token 羊毛进度或官方 API 计价口径。
- 验证：`python -m compileall -q app main.py`；CCS reader 回归测试 3 项通过。

## 2026-07-30 · 0.3.13 · 模型趋势偏好持久化与规格收口

- 模型活动范围和 Token/API 指标选择接入 `SettingsManager`，点击后立即写入现有配置文件，启动时恢复；旧配置兼容默认 30 天 + Token。
- #1～#5 已完成验收并关闭，保留 GitHub 评论中的测试、真实数据和 Windows 单实例证据。
- 验证：`python -m compileall -q app main.py`、`PYTHONPATH=. python -m pytest -q`（102 passed）和重启单实例检查。

## 2026-07-30 · 0.3.12 · Runtime 状态诊断

- 隐藏 Codex app-server Runtime 新增只读状态快照，覆盖未启动、初始化中、已就绪、响应超时、已断开和进程退出。
- 设置页数据源诊断展示当前 Runtime 状态与最近错误，帮助区分实时通道失败和 session 快照回退；不改变额度/任务的既有回退口径。
- 新增 Runtime 状态和错误快照回归测试；验证 compileall、完整 pytest 与 Windows 单实例重启。

## 2026-07-30 · 0.3.11 · 模型活动趋势与费用切换

- 模型视图新增 30/60/90/180 天活动范围按钮，明确显示实际日期边界，并按自然日补全空日期；不改变每日/每周/每月/累计现有统计口径。
- 模型趋势按活动范围展示 Top 8 与“其他模型”，叠加当前范围总量基线；Token/API 费用指标均支持 tooltip 和 0 基线。
- API 费用按精确模型 ID 的官方价格逐日计算，未知/内部模型保持未计价，其他模型按实际计价合并并显示覆盖率。
- 验证：`python -m compileall -q app main.py`、`PYTHONPATH=. python -m pytest -q`（100 passed）和 Qt offscreen 多序列渲染 smoke 通过。

## 2026-07-30 · 0.3.10 · 分支 token 去重与大数据保护

- 读取 `session_meta.parent_thread_id` 建立父子线程关系，按累计/本次 token 计数生成有限指纹，去除子线程继承的公共 token 前缀，并将前缀末端作为子线程累计高水位，避免首个新事件重放历史。
- JSONL 单行/单文件、app-server stdout、聚合内存缓存、Runtime 线程列表和任务列表增加硬上限；超限或异常时安全返回空结果，损坏行跳过，文件失败时清除旧文件缓存。
- SQLite 继续使用只读连接和 1 秒超时；rollout 文件级缓存继续最多保留 1024 项，不复制 transcript 正文到派生数据。
- 验证：`python -m compileall -q app main.py` 通过；`PYTHONPATH=. python -m pytest -q` 为 98 passed；Qt offscreen smoke 通过。

## 2026-07-30 · 0.3.09 · 额度 fail-closed 与展示边界收口

- Runtime 已返回但窗口未知、重复、畸形或部分时，禁止回退旧 session 额度；只有 Runtime 无响应才允许使用持久化快照。
- 归一化器继续保留已识别窗口供诊断，同时检查带 `windowDurationMins` / `window_minutes` 的额外字段并记录 `duplicate_count`；展示层统一隐藏不可验证窗口。
- 托盘月度重置时间改为按所选统计时区格式化；前三种圆形悬浮窗移除圆形轮廓外的月度摘要条，月度-only 仍使用圆内单额度布局，矩形样式保留摘要。
- 验证：`python -m compileall -q app main.py` 通过；`PYTHONPATH=. python -m pytest -q` 为 93 passed；Qt offscreen smoke 通过。

## 2026-07-30 · 0.3.08 · 额度窗口归一化

- 额度窗口按真实 `windowDurationMins` 识别：`300` 为 5h、`10080` 为 7d、28–31 天为月度，不再依赖 `primary/secondary` 槽位顺序。
- 新增 reset credit 次数与完整重置时间明细；0 或缺失时不绘制占位信息，月度额度独立保留，不误写入 7d。
- 主卡重置条、悬浮窗 tooltip 和托盘 tooltip 接入月度窗口；未知、重复、畸形窗口 fail-closed。
- 验证：`python -m compileall -q app main.py`；`PYTHONPATH=. python -m pytest -q`（87 passed）；Qt offscreen smoke；真实 Runtime 7D 100% 数据读取。

## 2026-07-30 · 0.3.07 · Runtime 断线与任务归档状态收口

- Runtime 进程断线、退出或响应失败时不再在 5 分钟 TTL 内返回旧额度，改为重新尝试或回退 session 快照。
- stdout reader 绑定所属连接的消息队列，避免旧连接 EOF 污染新连接。
- 任务完成优先使用 `archived_at`，旧 SQLite 结构才回退 `updated_at`。
- 验证：fake app-server 握手与单进程复用、断线回退、归档状态回归；`python -m compileall -q app main.py`；`PYTHONPATH=. python -m pytest -q`（81 passed）；Qt offscreen smoke 通过；真实 Runtime 返回 7D 100% 与北京时间 `08/05 12:43:25` 重置；重启后项目 `pythonw.exe` 严格 1 个、可见主窗口标题 `CodexUU`、隐藏 app-server 子进程 1 个；45 秒/8 次采样 PID 保持不变。

## 2026-07-30 · 0.3.06 · 隐藏额度 runtime 控制台

- 修复从 `pythonw.exe` 启动 `~/.codex/plugins/.plugin-appserver/codex.exe app-server --stdio` 时 Windows 弹出黑色终端的问题。
- Windows 子进程使用 `CREATE_NO_WINDOW`，不改变 app-server 的标准输入输出和额度读取逻辑。
- 验证：新增无窗口启动回归测试；`python -m compileall -q app main.py`；`PYTHONPATH=. python -m pytest -q`。

## 2026-07-30 · 0.3.05 · 定时刷新重启循环修复

- 修复后台 60 秒刷新每次都启动并终止独立 `codex app-server` 的问题；该行为可能使 Codex Desktop 的受管 runtime 被反复回收或拉起。
- 实时额度一旦成功读取，5 分钟内复用同一份已验证的窗口、百分比和重置时间；Token、任务、趋势、项目和工具等本机聚合仍保持原有每分钟刷新。
- 验证：新增 live quota 进程复用测试；完整测试通过后重启单实例验证。

## 2026-07-30 · 0.3.04 · 实时额度重置时间修复

- 修复 Windows Store 的 PATH 执行别名遮蔽 `~/.codex` 内独立 Codex runtime，导致应用只读取已耗尽的空 rollout 快照、无法显示真实重置时间的问题。
- 额度读取现在发现并优先调用可执行的独立 runtime，保持 app-server stdio 连接直至完成初始化和 `account/rateLimits/read` 响应，避免旧逻辑过早关闭 stdin 而丢失响应。
- 兼容新版 `rateLimitsByLimitId.codex` 多额度桶；当实时后端返回 100% 已用及 `resetsAt` 时，主卡、悬浮窗和托盘沿用真实 7D 重置时间，不再显示“等待后端重置”。
- 验证：`python -m compileall -q app main.py` 通过；`PYTHONPATH=. python -m pytest -q` 通过（74 passed）；本机 app-server 实时读取为 7D 已用 100%、北京时间重置 `08/05 12:43`。

## 2026-07-30 · 0.3.03 · 额度耗尽状态可见性修复

- 修复 Codex 在额度耗尽后把 `rate_limits.primary/secondary` 清空时，主界面和桌面悬浮窗只显示“暂不可用”的问题。
- 仅当官方快照明确带有 `limit_id=codex` 且 5h/7d 两个窗口均为空时标记“额度已用尽”；没有该明确形状时仍显示“暂不可验证”。
- 耗尽状态不伪造 5h/7d 百分比、空环或重置时间，保持缺失窗口动态隐藏，同时在主卡、悬浮窗 tooltip 和五种悬浮样式中给出状态文案。
- 验证：`python -m compileall -q app main.py`；`PYTHONPATH=. python -m pytest -q`。

## 2026-07-30 · 0.3.02 · 额度最新快照口径修复

- 修复额度读取器在最新 rollout 的 `rate_limits` 明确为空时继续扫描旧文件，导致界面显示过期 7d 额度的问题。
- 最新空快照现在立即生效；5h/7d 缺失时动态隐藏，不伪造或回退旧额度。
- 验证：`python -m compileall -q app main.py`；`PYTHONPATH=. python -m pytest -q`，71 passed。

## 2026-07-30 · 0.3.01 · Codex 单数据源与审计修复

- 移除 Claude Code 前后端：删除 transcript 读取与派生索引、设置数据源切换、Claude 额度/任务/项目/模型/工具/Skill 汇总；CodexUU 现在只读取并展示 Codex 本机记录。
- 修复审计问题：累计统计改读全量精细事件；模型价格只接受精确 ID；项目按完整目录聚合并为同名目录显示父目录区分；工具榜仅统计明确 `function_call` / `custom_tool_call`。
- 刷新流程改为 `QThread` + Signal，移除 50ms 轮询；桌面悬浮样式名称同步 A/C/C/B/B；Windows 构建脚本拒绝脏工作树。
- 验证：`python -m compileall -q app main.py`；`PYTHONPATH=. python -m pytest -q`。

## 2026-07-15 · 0.2.21 · 紧凑标准窗口与趋势轴线回归

### 必须修复

- 修复 `scripts/build_windows.ps1` 在 Windows PowerShell 5.1 下因无 BOM UTF-8 中文提示被错误解码而产生的语法错误；构建提示改为 ASCII，README 中的本机构建命令可直接执行。
- 发布构建生成的 `/build`、`/dist`、根目录 `*.spec` 与 `/tmp` 审计文件统一加入 `.gitignore`，避免安装包和临时截图误入源码提交。
- 将主窗口标准客户区从 `1180×800` 收紧到 `1060×720`，继续保持普通窗口等比缩放、最大化/全屏自由铺满；工具不再以过大的窗口作为避免错位的前提。
- 摘要区固定高度从 310px 收紧到 282px，额度卡宽度、指标卡最小高度、重置条和主面板边距同步压缩；节省出的空间优先留给下方 Tab，最小窗口不会靠吞掉趋势内容换取紧凑尺寸。
- UsagePlot 根据实时绘图区高度选择 Y 轴刻度：高图显示 0/中间值/最大值，短模型图空间不足时自动减少刻度但始终保留 0 基线，避免图 1 的数值叠字与裁切。
- X 轴标签统一下移到基线下方 8px 的独立区域；月度与累计的 12 个数据点逐月显示月份，不再用固定步长跳过隔月标签。
- 每日热力图的月份标签增加左右边界夹紧；最右侧 `YYYY/MM` 不再越过卡片而被截断。

### 影响范围

- 影响 Windows 主窗口标准尺寸、顶部摘要区密度和用量趋势概览/模型图的坐标轴排布；不改变额度、Token、模型、会话或日期范围的数据口径。

### 验证结果

- `python -m compileall -q app main.py`：通过。
- 定向 Qt 回归：标准客户区 `1060×720` 下用量趋势实际尺寸为 `1004×284`，内容栈高 175px；概览图高 137px、模型图高 68px，八种口径/视图组合的坐标标签均位于控件边界内。
- 月度与累计在概览和模型视图中均绘制 12 个逐月标签；热力图月份标签矩形全部包含于控件边界。`PYTHONPATH=. python -m pytest -q` 通过（72 项）。
- 本机 PyInstaller 6.21.0 按 `requirements.txt` 完整打包成功，`dist/CodexUU/CodexUU.exe` 可独立启动并显示可见主窗口；主 EXE 为 5.15MB，SHA-256 为 `94E87D1030C39A4D94AE9E69D64C2E3355E70F018F59F3522710F3F5C3B1EAE7`。
- `scripts/restart.ps1`：打包验收后成功恢复开发实例，PID 40304；标题为 `CodexUU` 的可见主窗口实例严格为 1。真实最小窗口已复核顶部摘要、任务与用量趋势；发现并修复热力图最右 `YYYY/MM` 被截断的问题。

## 2026-07-15 · 0.2.20 · 主窗口等比缩放约束

### 必须修复

- 将主窗口的标准客户区固定为 `1180×800`，并把该尺寸设为普通窗口硬下限；不再允许缩到旧的 `1060×720` 后压缩顶部指标、趋势卡片和模型详情。
- Windows 普通窗口通过原生 `WM_SIZING` 约束边缘与角落拖拽，始终保持 `1180:800` 设计比例；程序化 resize、贴靠恢复等非交互尺寸变化也会回正到等比尺寸。
- 最大化、全屏与最小化状态绕过比例校正，最大化时可完整使用屏幕，不会被强制回弹成带留白的比例窗口。

### 影响范围

- 仅影响 Windows 主窗口的最小尺寸、普通窗口缩放与最大化/全屏状态；不改变桌面悬浮窗缩放、各页面数据口径或控件内部布局。

### 验证结果

- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：通过（71 项）；新增 3 项覆盖标准硬下限、等比尺寸换算和四个主 Tab 在标准客户区内的父级边界。
- `scripts/restart.ps1`：成功重启，PID 26020，标题为 `CodexUU` 的可见主窗口实例严格为 1。
- Windows 原生客户区实测：请求 `1060×720` 被限制为标准 `1180×800`；请求非等比 `1400×900` 自动校正为 `1400×949`；最大化客户区使用完整 `1920×1017`，恢复后仍保持等比，最终回到标准尺寸。

## 2026-07-15 · 0.2.19 · 修复真实父级裁切

### 必须修复

- 确认上一版失败根因：UsagePlot 强制最小高度 210px、TokenHeatmap 强制最小高度 160px，但真实卡片只能分配更小高度；子控件因此撑大页面并被父级裁切，导致代码中已绘制的 0、月份和热力图最后一行在实际窗口中不可见。
- UsagePlot 最小高度降至 60px，并按当前真实高度重新计算绘图区；X 轴标签左右夹紧在控件内部，0 基线、Y 轴文字和月份标签全部保留在可见矩形内。模型页较矮的趋势图也使用相同规则。
- TokenHeatmap 最小高度降至 120px；单元格同时受当前宽度和高度约束，顶部位置从 34px 回调至 26px，底部固定保留约 10px，七行不再被卡片底部吃掉。

### 影响范围

- 影响用量趋势概览/模型图表和每日热力图的自适应绘制；不改变方案 B 范围条或任何 Token 数据。

### 验证结果

- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：通过（68 项）。
- Qt 真实尺寸回归：`UsageTrendWidget(1108×320)` 不再被最小尺寸扩张为 435px；概览图高 158px、模型图高 95px，均位于父级 200px 可见内容区内。轴标签矩形全部包含于绘图控件，热力图底部保留 8–12px。

## 2026-07-15 · 0.2.18 · 方案 B 范围条与趋势绘图边界

### 必须修复

- 用量趋势从方案 3 切换为方案 B：口径按钮恢复单行，新增独立通栏范围条。左侧统一为“统计范围 + 纯日期”，每日显示 `MM/DD`，每周和每月显示 `MM/DD-MM/DD`，累计显示“全部记录”；右侧统一为“数据更新 MM/DD HH:MM”。
- UsagePlot 统一扩大左/下绘图区边距，0 基线和 `0` 数值始终在卡片内部显示；月度及累计横轴月份改为两位月份标注，X/Y 轴文字不再贴住或越出边界。
- 模型趋势 tooltip 改为在指针上方弹出，避免靠近底部数据点时被窗口吞掉；所有折线和柱形仍严格以 0 为绘制基线。
- 每日 Token 热力图缩小水平间隙并放大单元格，月份标签保留在上方，网格整体下移，底部安全边距从 28px 收紧至约 6px。

### 影响范围

- 影响用量趋势的口径控制区、概览/模型图表绘制和每日热力图；不改变 Token、模型、会话或统计时间范围的数据来源。

### 验证结果

- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：通过（68 项）。
- Qt offscreen：覆盖方案 B 四种范围文案、八种视图组合、月度月份、0 基线预留和热力图网格占用。

## 2026-07-15 · 0.2.17 · 罗盘呼吸感与日期方案 3

### 必须修复

- 单 7D 罗盘中心的“7D 使用率”和百分比同时缩小，降低标签字重并重新调整两行基线，给圆心留出更充足的负空间；双额度中心的两组文案同步按比例收紧。
- 用量趋势正式采用日期排布方案 3：每日/每周/每月/累计仍在原位置左对齐，只有当前选中按钮显示日期副行；每日为 `MM/DD`，每周为周一至周日，月度为 `YYYY/MM`，累计为“截至 YYYY/MM/DD”。
- 数据更新时间使用所选统计时区，固定放在“概览/模型”切换左侧并提供含秒与时区的 tooltip；概览左卡移除重复的“范围 + 总量”角标，卡片内部不再抢占日期导航的信息层级。

### 影响范围

- 影响主界面额度罗盘中心排版和用量趋势的口径控制行；不改变 Token、额度、模型、会话、回合或统计时间范围的计算方式。

### 验证结果

- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：通过（66 项）。
- Qt offscreen：覆盖每日/每周/每月/累计 × 概览/模型八种组合，检查选中按钮副行、按钮尺寸、更新时间与视图切换边界。

## 2026-07-15 · 0.2.16 · 额度卡间距与单重置居中

### 必须修复

- 删除主界面额度卡右上角多余的“方案 C”设计徽章，只保留真实产品标题与额度信息。
- 降低罗盘的强制最小高度并扩大纵向布局间距，确保固定高度摘要区内罗盘绘制边界严格停在重置条上方，不再与时间框重叠。
- 单 7D 模式下，重置图标、标签和时间作为一个信息组在整条内水平居中；双额度模式下两组信息仍各自在半区居中。

### 影响范围

- 仅影响主界面额度卡的布局和单/双重置条对齐；不改变额度值、方向、重置时间或点击切换逻辑。

### 验证结果

- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：通过（64 项）。
- Qt offscreen：新增罗盘与重置条几何边界及单重置区水平中心回归检查。

## 2026-07-15 · 0.2.15 · 主界面方案 C 原型收口

### 必须修复

- 清除主界面额度卡中不属于原型的“7D 单层罗盘”说明；标题固定为“额度使用情况”，右上固定显示“方案 C”，不再把已用/剩余口径误显示为设计标签。
- 双额度罗盘中心改为 5H 与 7D 分层的“使用率 + 数值”排布并加入中线；单 7D 保持居中的“7D 使用率 + 数值”。外紫 7D、内蓝 5H、完整灰色底轨和底部起点方向不变。
- 底部重置区改为贴近原型的圆角重置条：双额度呈 5H/7D 两栏并使用分隔线，只有 7D 时自动隐藏空栏并让 7D 占满整条；完整时间仍使用统计时区并保留秒级 hover 提示。

### 影响范围

- 仅更新主界面额度卡的展示和离屏回归；不改变额度读取、已用/剩余切换、任务归档或统计数据口径。

### 验证结果

- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：通过（64 项）。
- Qt offscreen：覆盖双额度和单 7D 的重置条显隐、方案 C 徽章、中心点击切换；下一步重启可见主窗口复核。

## 2026-07-15 · 0.2.14 · 主界面方案 C 落地与项目归档

### 必须修复

- 主界面额度区不再沿用旧的小环卡。额度卡扩大为方案 C 的完整结构：标题与当前口径徽章、居中的双层/单层罗盘、底部动态重置区域；双额度维持外紫 7D、内蓝 5H，单 7D 自动扩大为单层。
- 圆心点击继续切换已用/剩余，原有数据口径与设置同步保持不变；摘要区高度相应扩展，避免更大的罗盘挤压重置区。
- 完成列改为按 Runtime + 项目聚合：同项目的多条归档线程合并为一张项目卡，卡片显示最新归档摘要与线程数，并按最新归档时间从近到远排列。

### 影响范围

- 影响主界面顶部额度区及今日任务完成列的展示层级；不修改额度、Token、归档原始记录或项目数据源。

### 验证结果

- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：通过（63 项）。
- Qt offscreen：已渲染方案 C 的主界面双额度罗盘，确认环层顺序、中心切换和双重置区；归档项目聚合回归覆盖同项目多归档线程。

## 2026-07-15 · 0.2.13 · 20% 无损缩放与主界面方案 C

### 必须修复

- 悬浮窗“小”预设改为中号 20%，自定义范围保持为 20%–300%。在 20%–55% 使用严格等比的基准画布缩放，避免最小尺寸下的字体下限补偿挤压轨道、圆环与重置区域。
- 主界面额度窗口落地方案 C：双额度时外层固定绘制紫色 7D、内层固定绘制蓝色 5H；两条环均保留完整灰色底轨，已用自底部沿左侧、剩余自底部沿右侧。
- 额度罗盘圆心加入点击切换，剩余与已用在主界面、悬浮窗和持久化设置间同步；5H 与 7D 的重置时间继续动态显示，单额度不保留空内环。

### 影响范围

- 影响桌面悬浮窗缩放、主界面额度窗口的圆环顺序与交互；不修改额度原始值、数据源或统计口径。

### 验证结果

- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：通过（63 项）。
- Qt offscreen：已实渲染方案 C 双额度卡，确认外紫 7D、内蓝 5H、两个重置时间和中心点击切换；五种悬浮窗覆盖 20% / 63% / 137% / 300% 缩放，无绘制越界。

## 2026-07-15 · 0.2.12 · 连续缩放、模型日口径与北京时间

### 必须修复

- 悬浮窗“小”预设收紧至中号的 55%；新增 55%–300% 自定义缩放，设置页可精确保存，右键菜单和 `Ctrl + 滚轮` 可连续以 5% 调整。所有比例继续共用同一画布、字体、热区和屏幕边界校正映射。
- 模型页“每日”从错误的近 30 天趋势窗口改为所选统计时区的实际当天；模型行与详情元信息改为显示“本日/本周/本月 + 日期范围”。使用本机真实数据复核后，当天列表仅包含 `gpt-5.6-sol` 与 `gpt-5.6-terra`，不再错误显示历史 gpt-5.5。
- 项目排行和任务卡的活动时间统一经统计时区转换；当前默认北京时间下，UTC `03:18` 显示为 `11:18`。完成列改为归档历史：保留所有明确归档的线程，并以归档时间从最近到最晚排序。

### 影响范围

- 影响悬浮窗尺寸设置与绘制、模型统计口径、项目/任务时间显示和完成任务列表；不改变原始日志、额度数据、模型定价或运行时数据源。

### 验证结果

- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：通过（62 项）。
- Qt offscreen：覆盖五种悬浮窗 × 单/双额度 × 小/中/大，以及 55% / 63% / 137% / 300% 的连续缩放渲染，未见边界或热区失配。

## 2026-07-15 · 0.2.11 · 刻度简化与重置时间保护

### 必须修复

- 双环仪表 C 移除环绕的 `0 / 25 / 50 / 75` 数字，仅保留进度刻度线和底部起点方向。
- 状态胶囊 B 为重置日期和时间扩展独立右侧双行区域；额度轨道从 `5H/7D` 标签正下方开始，不再横向挤压时间信息。
- 双轨卡片 B 同样采用独立的右侧双行重置区域，并收紧双额度行距，避免第二条轨道在卡片底部越界。
- 小尺寸继续收紧至中号的 68%（圆形窗约 170×170），时间文字的物理字号下限仍为 9 px。

### 影响范围

- 仅调整桌面状态悬浮窗的视觉排版与尺寸映射；不改额度数据、显示口径、设置字段或手势交互。

### 验证结果

- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：通过（59 项）。
- Qt offscreen：已逐项复核五种样式 × 小/中/大 × 单 7D/5H+7D 的浅色渲染，未见文字、重置区、轨道或圆环越界、偏移。

## 2026-07-15 · 0.2.10 · 小号可读性与圆心密度

### 必须修复

- “小”尺寸由中号的 86% 收紧至 74%，使桌面悬浮窗在最小档具有明确的体积差异。
- 字体按当前画布缩放反向补偿：重置日期和时间的实际绘制字号不低于 9 px，避免随最小档一起缩至不可读。
- 信息圆盘、双环仪表和极简圆环的单额度状态扩大圆环、加粗轨道，并放大圆心标签、百分比和重置时间，减少中心无效留白。

### 影响范围

- 仅影响桌面状态悬浮窗的尺寸映射、字体最小可读性和单额度圆环绘制；不改额度数据、显示口径或交互行为。

### 验证结果

- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：通过（59 项）。
- Qt offscreen：已复核单 7D 的小/中尺寸和五种样式渲染；小号圆形窗为 185×185，重置文字保持不少于 9 px 的实际字号。

## 2026-07-15 · 0.2.09 · 悬浮窗留白与基线复核

### 必须修复

- 基于实际窗口截图，单 7D 的信息圆盘、双环仪表和极简圆环不再沿用双额度的内缩半径；三种样式均扩大有效圆环，并重新上移标签与数值基线，避免圆内出现大面积无效留白。
- 状态胶囊与双轨卡片统一为紧凑、同档占用接近的中号画布；单额度轨道采用独立垂直起点，收紧标题分隔线、额度标签、进度轨道与重置时间之间的距离。
- 同步更新几何回归断言，确保卡片尺寸和点击热区仍使用同一基准画布缩放。

### 影响范围

- 仅调整桌面状态悬浮窗五种样式的绘制几何与测试期望；不改动额度数据、显示口径、交互手势或设置持久化。

### 验证结果

- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：通过（58 项）。
- Qt offscreen：已复核五种样式的单 7D / 5H+7D 浅色渲染，单额度圆环、胶囊和双轨的有效内容均已收紧且未发生越界。

## 2026-07-15 · 0.2.08 · 原型复核与布局二次校正

### 必须修复

- 依照实际窗口截图复核并拉开信息圆盘 A、双环仪表 C、极简圆环 C 在单 7D 下的视觉层级，避免三种圆环退化为同一布局。
- 双环仪表 C 补齐圆内数字刻度：底部为 `0` 起点，右侧 / 顶部 / 左侧依次为 25 / 50 / 75；进度刻度和圆环均从该底部起点按已用/剩余口径绘制。
- 状态胶囊 B 重排“今日 / 轨道 / 重置”三栏，7D 重置日期与时间按两行展示；双轨卡片 B 扩大标题和轨道行距，去除额外口径文字。

### 影响范围

- 仅影响桌面状态悬浮窗的绘制细节与可读性，不改数据来源、窗口交互或设置字段。

### 验证结果

- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：通过（58 项）。
- Qt offscreen：逐项对照五种样式的单 7D 与 5H+7D 渲染，额外检查小/中/大缩放、圆环底部起点和重置文本边界。

## 2026-07-15 · 0.2.07 · 五种悬浮窗按选定原型重构

### 必须修复

- 信息圆盘采用方案 A：去除“今日”字样，双额度与单 7D 均在圆内重新居中排布。
- 双环仪表采用方案 C：圆内刻度双环，紫色外环固定表示 7D、蓝色内环固定表示 5H；极简圆环采用方案 C 的圆内双值分栏。
- 状态胶囊采用方案 B 的紧凑轨道带，双轨卡片采用方案 B 的整合标题与纵向双轨；两者不使用圆环。
- 所有圆环只在圆内显示信息，灰色表示未使用轨道/刻度；已用自底部沿左侧、剩余自底部沿右侧，缺失 5H 不绘制空环或空刻度。
- 五种样式重新校准基准画布，三档尺寸均以同一画布缩放，且同档整体占用保持接近。

### 影响范围

- 仅影响桌面状态悬浮窗五种样式的绘制、基准尺寸与额度点击热区；不改数据源、设置持久化字段或主窗口布局。

### 验证结果

- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：通过（58 项）。
- Qt offscreen：五种样式已覆盖小/中/大、5H+7D、单 7D、浅/深主题渲染；圆环起点按像素绘制验证为底部起点。

## 2026-07-15 · 0.2.06 · 悬浮窗三档尺寸统一缩放

### 必须修复

- 修复“小 / 中 / 大”仅缩放外框、内部仍使用中号绝对坐标的问题。五种桌面悬浮窗现在均以各自中号基准画布绘制，再通过同一 X/Y 映射缩放圆环、分隔线、徽章、字体与阴影。
- 点击额度中心的热区改用同一基准坐标反向映射，避免缩放后视觉中心与实际点击区域脱离。
- 渲染回归覆盖五种样式 × 三档尺寸 × 单/双额度 × 浅/深主题；新增三档尺寸的点击热区回归测试。

### 影响范围

- 仅影响桌面状态悬浮窗的绘制与额度中心点击命中，不改额度数据、样式设置、持久化字段或主窗口布局。

### 验证结果

- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：通过（57 项）。
- Qt offscreen：实际生成五种样式在小/中/大三档的双额度渲染对照，未见内容越界或比例错位。

本文件记录本地复刻项目的每次有效更新。每次代码更新完成后，需同步维护根目录 `AGENTS.md` 与本文件，并重启程序。

## 2026-07-14 · 0.2.05 · 五种悬浮窗按定稿图重构

### 必须修复

- 信息圆盘 A 从旧纯圆窗改为圆角卡片：双额度采用左侧双环、右侧 5H/7D 数值和重置时间，单 7D 自动改为居中大环与底部今日用量。
- 双环仪表 A 改为纵向仪表卡，额度值位于环心，底部独立展示重置图例和今日 Token；单额度不保留 5H 空环。
- 极简圆环 B 改为横向分栏卡，左侧额度环、右侧今日 Token；双额度状态在右侧分别显示 5H 与 7D 重置时间。
- 状态胶囊 B 补齐双额度环心信息和带语义色的额度标签，双轨卡片 B 固定为左侧今日用量、右侧动态单/双轨及时间标签。
- 设置页的悬浮样式名称同步为五个定稿名称；五种样式的额度中心点击热区按新卡片布局重新计算。

### 影响范围

- 桌面悬浮窗五种样式、设置页样式选择、单/双额度动态布局、浅色/深色主题和点击口径切换。

### 验证结果

- `python -m compileall -q app main.py` 通过。
- `PYTHONPATH=. python -m pytest -q` 共 56 项通过。
- 五种样式均完成浅色/深色、5H+7D、单 7D 的真实 Qt 渲染检查；重启后确认只运行 `0.2.05` 单实例。

## 2026-07-14 · 0.2.04 · 五种悬浮窗定稿与点击响应

### 必须修复

- 按确认方案定稿五种桌面悬浮窗：信息圆盘 A、双环仪表 A、极简圆环 B、状态胶囊 B、双轨卡片 B；胶囊与双轨适当扩大画布，重新分配今日用量、额度和重置时间的层级。
- 所有圆环统一绘制完整灰色底轨，只以颜色表达真实比例；双额度固定为紫色 7d 外环、蓝色 5h 内环，缺失 5h 时自动收敛为单 7d 环，不显示空环或占位行。
- 统一额度方向：已用从底部沿左侧增长，剩余从底部沿右侧增长；主页面、通知区与桌面悬浮窗继续共享同一显示口径。
- 悬浮窗按下时立即提供轻量缩放反馈，单击判定窗口从系统双击间隔缩短为 220ms；中心单击切换已用/剩余、其他区域单击恢复主窗口、双击最小化主窗口的动作保持互斥。
- 移除模型详情中的厂商/官方价格匹配/统计口径说明徽章；金额是否可计价保持不变，精确价格来源移动到金额 tooltip，未知模型继续显示“未计价”。

### 影响范围

- 桌面悬浮窗五种样式、单/双额度动态布局、浅色/深色主题、悬浮窗点击手势与模型详情卡。

### 验证结果

- `python -m compileall -q app main.py` 通过。
- `PYTHONPATH=. python -m pytest -q` 共 52 项通过。
- 浅色/深色五样式、单/双额度 Qt offscreen 渲染通过；更新后按项目脚本重启并确认单实例。

## 2026-07-14 · 0.2.03 · 额度方向、模型口径与多厂商计价

### 必须修复

- 主页面、通知区图标与五种桌面悬浮窗统一为底部起点：已用沿左侧增长，剩余沿右侧增长；后端真实额度百分比保持不变。
- 悬浮窗点击改用系统双击间隔判定：单击中心切换已用/剩余，单击其他区域恢复主窗口，双击最小化主窗口，避免双击先误触发单击动作。
- 模型统计不再只切换趋势图。每日、每周、每月、累计现在同步重算模型排行、Token 拆分、会话、回合、占比与金额；会话和回合按最后活动时间进入当前窗口。

### 模型与计价

- 模型页左栏改名为“模型使用量”；右栏增加厂商/计价状态、本期范围、未缓存/缓存/输出三项层级卡、会话、回合、占比与最近活跃信息，减少无意义空白。
- 新增顶栏“GPT / 全部”范围切换并持久化。GPT 只汇总日志中明确的 `gpt-` 模型；全部包含第三方模型。顶部今日/本周/本月/累计、羊毛进度、趋势和模型页均使用同一范围。
- 接入 DeepSeek V4 Flash/Pro 与 Xiaomi MiMo 2.5/2.5 Pro 的官方公开输入、缓存输入和输出价格；仅精确模型 ID 计价，内部模型和第三方网关别名继续标记为未计价。

### 验证

- 真实本机模型日志可识别 GPT、DeepSeek、MiMo 与内部模型；DeepSeek V4 Flash 本机样本按官方价格重新计算为 100% 计价覆盖，整体本机精细模型计价覆盖约 99.86%。
- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：51 项通过，新增第三方官方价格、模型窗口会话/回合、模型范围持久化和悬浮窗单/双击互斥测试。
- Qt offscreen：使用真实本机模型数据验证模型详情与 GPT/全部切换；更新后按规范重启并确认严格单实例及真实主窗口可见。

## 2026-07-14 · 0.2.02 · 悬浮窗交互统一与模型用量视图

### 必须修复

- 主页面、通知区图标和桌面悬浮窗统一额度方向：已用从底部沿左侧增长，剩余从顶部沿右侧增长；只改变显示方向与百分比口径，不改动后端真实额度值。
- 桌面悬浮窗改为单击额度中心切换已用/剩余，单击其他非拖动区域恢复主窗口；点击判定使用真实圆形区域，避免圆角透明区域误触。
- 信息圆盘恢复完整 360° 比例表达并放大重置时间；双环仪表补充 7D 重置时间，状态胶囊强化重置层级，双轨卡片在缺失 5h 时仅保留单条 7D 轨道。五种样式均复核小/中/大尺寸及单/双额度状态。

### 模型用量与视觉

- Codex 从真实 `turn_context` / `thread_settings_applied` 读取模型、推理强度与回合 ID，并关联后续 `token_count` 增量；新增按“模型 + 强度”聚合的 Token、会话、回合和每日趋势。Claude 沿用本机索引中的真实模型，缺失强度明确显示“未提供”。
- “用量趋势”新增“概览 / 模型”二级视图；模型排行和右侧选中模型趋势同步每日、每周、每月、累计口径，不改变主页面四个固定 Tab。
- Token 组成横条由 6px 提升为 8px，并为非零低占比分段提供最小可见宽度；未缓存/缓存/输出文字提升至 9px，同时提高浅色与深色主题对比。
- 精细 Token 增量增加单轮聚合缓存：每日统计完成后模型统计直接复用同一批归一化事件。本机实测冷聚合约 7.1 秒，随后模型分组约 0.04 秒，避免同一轮刷新重复扫描。

### 验证

- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：50 项通过，新增模型/强度关联、回合与每日归属、悬浮窗弧线起点和中心/非中心点击测试。
- Qt offscreen：完成五种悬浮样式 × 三档尺寸的浅色单 7D 对照、深色双额度对照，以及使用真实本机模型数据的模型视图渲染。
- 更新后使用 `scripts/restart.ps1` 重启，并确认项目 `main.py` 严格单实例、真实主窗口可见。

## 2026-07-14 · 0.2.01 · 额度方向统一、移除快速状态与新增悬浮样式

### 必须修复

- 桌面悬浮窗与主页面统一额度方向：圆环从顶部为共同起点，已用沿左侧逆时针增长，剩余沿右侧顺时针增长；信息圆盘、双环仪表、极简圆环和通知区动态图标使用同一规则。
- 完整删除托盘快速状态弹窗的 Runtime 卡片、弹窗类、菜单入口、数据更新链路和主题样式；托盘单击与双击现在都直接恢复主窗口。
- 主窗口增加统一 `show_and_activate()` 恢复入口：兼容隐藏和最小化状态，并在 Windows 上补充 `SW_RESTORE`、窗口前置和前台激活，修复轻量模式下主界面无法重新打开的问题。

### 悬浮窗扩展

- 保留信息圆盘、双环仪表和极简圆环，并新增“状态胶囊”：左侧紧凑额度环，右侧显示 Runtime、口径、额度窗口重置时间和今日 Token。
- 新增“双轨卡片”：使用 5h 蓝色横轨与 7d 紫色横轨并列展示，完全区别于圆环信息架构；当前缺失 5h 时只渲染 7d，不保留空轨道。
- 五种悬浮样式均支持浅色/深色主题、小/中/大尺寸、拖动、右键切换和中心口径切换；设置页中英文选项与持久化白名单同步扩展。

### 验证

- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：48 项通过，覆盖新增样式几何、额度方向、中心切换和样式持久化。
- Qt offscreen：浅色主题同时渲染 5 种样式的已用/剩余双额度状态，矩形样式边界与动态退化通过。
- 更新后使用 `scripts/restart.ps1` 重启，并确认严格单实例、真实主窗口可见、隐藏/最小化后的托盘与快捷键恢复路径可用。

## 2026-07-14 · 0.1.10 · 项目任务聚合、双额度悬浮窗与轻量模式修复

### 必须修复

- 今日任务由“每条对话一张卡”改为按 Runtime + 项目聚合：项目卡显示项目名、最新线程摘要、线程数和最后时间；同一项目状态按进行中、待处理、定时、完成依次优先，完成列和顶部总数均改为项目口径。
- 项目卡使用宽度感知省略标签，完整标题保留在 tooltip；移除无意义的线程 ID 占位，修复图 1 进行中卡片标题截断且没有省略反馈的问题。
- 轻量模式移除会破坏 Windows 标准标题栏的 `WS_EX_TOOLWINDOW`，改用 Shell 官方 `ITaskbarList::DeleteTab` 隐藏任务栏入口；显式保留系统菜单、最小化、最大化和可调整边框。原生消息验证最小化、恢复和关闭隐藏均可用。
- 重启脚本的窗口探针增加 800×600 主窗口尺寸门槛，避免把同样显示 `CodexUU` 标题的圆形悬浮窗误认成主窗口。

### 悬浮窗与指标卡

- 信息圆盘改为 270° 仪表弧，只呈现优先额度窗口、当前口径和重置时间；双环仪表改为真实 5h 蓝色外环 + 7d 紫色内环，两窗口同时存在时分别显示百分比，缺失 5h 时自动退化为 7d 单环；极简圆环继续保留最少信息。
- 悬浮窗圆心单击可在“剩余 / 已用”间切换，立即写入全局设置并同步主页面额度卡、托盘动态图标与 tooltip。圆面移除今日 Token 等易冲突文字，详细数据继续在 hover 中完整展示。
- 今日/本周/本月/累计 Token 卡新增蓝/紫/橙组成横条，未缓存、缓存、输出改为三行纵向标签和值，解决窄卡片内三项被压缩的问题，并将顶部摘要区高度调整到 286px。

### 真实数据与验证

- 本机真实任务从 25 条今日线程聚合为 6 个项目：2 个进行中项目、4 个完成项目；其中 HyperFrames 2 条、工作服设计 4 条、八璟意向 13 条、八璟 4 条完成线程分别收敛为单项目卡。
- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：47 项通过，新增项目聚合和悬浮窗双额度/中心切换测试。
- Qt offscreen：使用真实 7d 额度及构造的 5h + 7d 双额度验证三种悬浮样式；真实仪表盘验证任务摘要为 6 个项目，顶部指标卡高度 148px。
- Windows 原生样式验证：主窗口保留 `WS_SYSMENU / WS_MINIMIZEBOX / WS_MAXIMIZEBOX / WS_THICKFRAME`，无 `WS_EX_TOOLWINDOW`；最小化、恢复、关闭隐藏均通过。更新后已重启并确认严格单实例及真实主窗口可见。

## 2026-07-14 · 0.1.09 · 桌面悬浮窗重做与设置页适配

### 必须修复

- 桌面悬浮窗不再通过透明窗口的调色板猜测深浅色，改为由应用主题管理器显式同步当前有效主题；浅色主题使用白色材质、浅灰轨道和深色文字，深色主题使用深色材质与高对比文字。
- 重绘信息圆盘、双环仪表、极简圆环三种样式，重新划分 Runtime、额度窗口、百分比、今日 Token 和重置时间的文本区域，并使用文本省略保护，解决圆环与文字重叠、底部吃字问题。
- 设置页“通用”Tab 改为内部滚动布局，底部取消/保存栏保持固定；最小尺寸提高到 660×600，修复快捷键、悬浮样式、轻量模式和关闭行为在窗口较小时被裁切。
- 修复 Windows 开发态偶发“`pythonw.exe` 存活但没有任何顶层窗口”的假启动：主窗口改为进入 Qt 事件循环后显示，并在 1.2 秒后使用原生 HWND 可见性再确认；重启脚本现在最多等待 8 秒验证标题为 `CodexUU` 的可见窗口，失败会终止残留进程并明确报错。

### 交互与口径

- 新增悬浮窗小/中/大三档尺寸，设置后持久化；桌面悬浮窗右键菜单可直接切换三种样式、三档尺寸、打开主窗口或隐藏，拖动位置继续持久化并进行屏幕边界校正。
- 今日任务四列标题补充完整 hover 口径：进行中为今天近 2 小时活跃的未归档线程，待处理为今天活动后超过 2 小时未更新的未归档线程，定时只认本机启用的 Automation，完成只认今天明确归档的线程。
- 真实本机审计：SQLite 共 58 条线程，其中 8 条历史归档、今天归档 0；当前仅 1 条线程在近 2 小时活动，且 `~/.codex/automations` 不存在，因此当前任务看板的 `1 / 0 / 0 / 0` 为真实口径结果。

### 验证

- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：45 项通过。
- Qt offscreen：使用真实本机额度数据验证浅色/深色主题下三种悬浮样式均可渲染；设置页在 660×600 下启用内部滚动，快捷键和悬浮窗尺寸字段拥有完整可用宽度。
- 更新完成后已通过 `scripts/restart.ps1` 重启，并确认项目 `main.py` 的 `pythonw.exe` 严格为 1 个实例。

## 2026-07-14 · 0.1.08 · 原生窗口修复、额度通道收敛与圆形状态窗

### 必须修复

- 移除羊毛进度的低频涌动高光循环，仅保留一次性的数值/进度缓动，避免暗色界面中的不连续亮线与空闲定时器开销。
- 轻量模式不再把主窗口改为 `Qt.Tool`：主窗口恢复 Windows 标准最小化、最大化、关闭按钮；仅通过 `WS_EX_TOOLWINDOW` 尝试隐藏任务栏入口，并在深色/浅色主题切换后重新应用 DWM 标题栏颜色。仪表盘顶部重复的关闭按钮已移除。
- 设置存在未保存草稿时，关闭或取消将先提示确认；桌面悬浮样式也纳入“保存设置后生效”的草稿流程。

### 数据与桌面体验

- 核验本机 Windows Store 版 Codex：其 `WindowsApps` 内嵌 `codex.exe` 被子进程启动策略拒绝，不能可靠地作为 stdio app-server。额度因此使用最新 session 中官方写入的 `rate_limit` 快照；读取仅扫描最近 14 天、按修改时间最多 32 个 rollout 文件，并复用文件级解析缓存，避免全历史扫描和刷新阻塞。独立 CLI 可用时仍优先 app-server。
- 数据源诊断明确区分实时 app-server 与 Store 桌面版 session 快照回退，不再把受限安装误报成“执行别名”。
- 桌面状态窗重构为圆形信息窗：提供“圆环 / 光晕双环 / 极简圆形”三种持久化样式，显示当前 Runtime 的真实可用额度、今日 Token 与重置时间；移除横向卡片、打开/隐藏按钮，改为拖动定位、双击打开主窗口与 hover 详情。
- 手动 GitHub Actions 构建无论是否发布 Release 都上传安装器及 SHA-256 Artifact（保留 14 天）；日常推送不触发构建或发布。

### 验证

- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：44 项通过。
- Qt offscreen：验证主窗口仍为普通 `Qt.Window`、三种圆形悬浮样式尺寸可创建。
- 已通过 `scripts/restart.ps1` 重启，并确认项目 `main.py` 的 `pythonw.exe` 仅有 1 个实例（PID 21068）。

## 2026-07-14 · 0.1.07 · 设置草稿、轻量模式与用量跳转

### 设置交互

- 所有偏好项改为“选择 / 编辑 → 保存设置 → 统一生效”：语言、主题、Runtime、更新偏好、额度口径、提醒阈值、统计时区、全局快捷键、桌面状态、轻量模式和关闭行为均不再在控件变化时立即写入。
- 新增“取消”与“保存设置”操作；取消会恢复当前已保存值。全局快捷键仅在保存时尝试原生注册，失败时保留旧组合键。
- 设置页所有自绘下拉框忽略鼠标滚轮，避免指针经过提醒阈值等控件时无意改值；点击弹层后才允许选择。

### 桌面与导航

- 新增默认开启的轻量模式：主窗口改用 Windows 工具窗口类型，运行时不显示任务栏按钮；通知区、全局快捷键和桌面状态窗仍可唤回主窗口，设置中可关闭轻量模式恢复常规任务栏行为。
- 顶部今日 / 本周 / 本月 / 累计 Token 卡片可点击，分别跳转到趋势页的每日 / 每周 / 每月 / 累计口径。
- 羊毛进度增加低频涌动高光（650ms 粗粒度定时器），只在有进度且未启用“减少动态效果”时运行；减少动态效果会立即停止该动效。

### 本机口径审计与验证

- 当前真实 Codex 数据核对：今日、本周、本月卡片与按自然日聚合的趋势值完全一致；累计精细 Token 与 `state_5.sqlite` 线程索引总量一致，均为 762.9M 左右（随实时使用变化）。当前检测到 8 个真实项目和 27 个活跃日期；Claude 本机尚无 transcript，因此正确显示为 0。
- `python -m compileall -q app main.py`：通过；`PYTHONPATH=. python -m pytest -q`：43 项通过。
- Qt offscreen 验证提醒阈值在保存前不改变配置、保存后生效；轻量模式主窗体使用 `Qt.Tool`、卡片跳转到月度趋势、羊毛进度动效及“减少动态效果”停止逻辑均正确。
- 已通过 `scripts/restart.ps1` 重启，并确认最终仅保留一个指向本项目 `main.py` 的 `pythonw.exe` 实例（PID 26032）。

## 2026-07-14 · 0.1.06 · 手动发布与桌面状态悬浮窗

### 开发与发布策略

- Windows Release 工作流取消 `v*` 标签自动触发；日常开发只推送代码和标签，不产生安装包或 GitHub Release。
- 工作流改为手动运行：可指定要构建的 Git ref；只有明确勾选发布并填写 Release 标签时，才会创建 GitHub Release。未填写发布参数时只构建，不发布。

### 桌面状态

- 新增默认显示的桌面状态悬浮窗，清晰展示当前 Runtime、今日本机 Token、5h / 7d 剩余比例及实际重置时间；不使用虚构额度或云端账号数据。
- 悬浮窗保持置顶且不抢焦点，可拖动；拖动后的坐标会持久化。恢复时按保存位置所在显示器或鼠标所在显示器进行边界校正，适配多显示器变化。
- 设置“窗口与快捷键”与托盘右键菜单均可即时显示/隐藏悬浮窗；悬浮窗内可打开主窗口或隐藏自身。

### 验证

- `python -m compileall -q app main.py`：通过；`PYTHONPATH=. python -m pytest -q`：42 项通过。
- Qt offscreen 验证设置开关、托盘菜单、桌面悬浮窗的创建、真实额度格式化与位置恢复逻辑；保存位置 `(100, 100)` 能正确转换为 Qt 坐标并显示。
- 已通过 `scripts/restart.ps1` 重启，并确认最终仅保留一个指向本项目 `main.py` 的 `pythonw.exe` 实例（PID 17436）。

## 2026-07-14 · 0.1.05 · 额度提醒、数据维护与多显示器托盘

### Windows 产品化

- 系统设置新增额度提醒阈值（关闭 / 10% / 20% / 30% / 50%，默认 20%）。提醒始终依据真实“剩余比例”判断，不受前端“已用 / 剩余”显示切换影响。
- 同一 Runtime 的 5h 或 7d 额度窗口在同一个重置周期只会显示一次 Windows 通知；额度恢复到阈值以上后才允许在后续再次低于阈值时提醒，避免 60 秒自动刷新造成通知轰炸。
- 托盘图标被收进 Windows 隐藏区而无法获得几何位置时，快速状态窗改按鼠标所在显示器定位；最终坐标按该屏幕可用工作区约束，兼容多显示器、任务栏和边缘位置。

### 数据维护

- 系统设置新增可滚动的“数据维护”卡片，显示 Claude 派生索引状态，并可在确认后清理索引；清理仅删除 `analytics.sqlite` 与 WAL/SHM 辅助文件，下次读取自动重建。
- 原始 Codex session、Claude transcript、项目目录和用户配置均不会被清理；维护操作在文案和确认对话框中明确说明范围。

### 验证

- `python -m compileall -q app main.py`：通过；`PYTHONPATH=. python -m pytest -q`：41 项通过。
- Qt offscreen 验证系统设置的阈值选择、索引维护入口与托盘快速状态窗均可创建；系统设置内容在 640px 高窗口内通过滚动承载，不出现裁切。
- 已通过 `scripts/restart.ps1` 重启，并确认最终仅保留一个指向本项目 `main.py` 的 `pythonw.exe` 实例（PID 14504）。

## 2026-07-14 · 0.1.04 · 项目详情、模型拆分与项目级导出

### 项目数据浏览

- 项目排行和“最近活跃项目”行保持原有双栏布局，现可直接点击打开轻量项目详情窗，不新增独立主页面。
- 详情窗按当前“本周 / 本月 / 累计”口径展示 Token、API 等效价值、计价覆盖率和会话数量；模型拆分使用日志实际模型，未知模型仍按未计价处理。
- 会话列表提供模型/会话 ID 筛选，并显示最近活动时间和主要模型；Codex 与 Claude Code 都复用各自项目精细事件，避免按总量比例分摊。

### 导出与边界

- 项目详情支持导出当前项目 JSON、会话 CSV，以及复制 Markdown 摘要。导出只含项目名、口径、派生 Token/价格、模型和会话摘要，不含 transcript 正文、提示词、工具参数或项目文件。
- Codex 读取 rollout 事件时补齐模型与会话聚合；Claude Code 则直接基于 0.1.03 的派生本机索引生成同一结构。

### 验证

- `python -m compileall -q app main.py`：通过；`PYTHONPATH=. python -m pytest -q`：40 项通过。
- Qt offscreen 使用真实本机 Codex 数据创建项目排行和项目详情窗：识别到 8 个真实项目、本周 2 个有用量排行项目，详情窗可创建并读取当前口径数据。

## 2026-07-14 · 0.1.03 · 增量索引、数据可信度与 Windows 发布链路

### 本机增量索引

- 新增 `~/.codexU/analytics.sqlite`，首次读取 Claude transcript 后建立派生统计索引；后续刷新仅重新解析大小或修改时间变化的 JSONL 文件。
- 索引只保存文件指纹、时间、模型、Token、工具和 Skill 名称；不保存消息正文、提示词、工具参数或项目文件内容。
- Claude 的 Token、趋势、项目、工具和 Skill 统计统一改读索引，单轮仪表盘刷新不会重复扫描同一批 transcript。

### 数据可信度与发布

- 设置页“数据源诊断”新增 CodexUU 本机索引状态、文件数、派生事件数、最后扫描时间与本机数据范围说明。
- 新增 Windows 发布脚本、Inno Setup 安装器定义和 GitHub Actions 工作流；推送 `v*` 标签后将自动构建 `.exe` 安装器与 SHA-256 校验文件并发布到 GitHub Release。

### 验证

- 增量索引测试覆盖首次扫描、文件变更重建、派生字段与不保存 transcript 正文。
- `python -m compileall -q app main.py`：通过；`PYTHONPATH=. python -m pytest -q`：39 项通过。
- Qt offscreen 验证设置页“系统”页、索引状态与本机数据范围说明可创建；实际本机无 Claude transcript 时索引正确显示为 0 文件 / 0 事件。
- 已执行单实例重启，最终 `pythonw.exe main.py` 实例数为 1。

## 2026-07-13 · 0.1.02 · 任务口径、趋势排版与稳定性审计

### 必须修复

- 修复每日 Token 活动月份文字使用基线坐标导致上沿被裁切的问题，改为固定标题矩形垂直居中绘制。
- 项目活动概览的“最近活跃”列表左侧补齐项目 SVG 图标，图标尺寸和行高保持一致。
- 对齐上游 codexU 的任务逻辑：“完成”只统计今天归档的线程，优先使用 `archived_at`；进行中窗口由 15 分钟修正为 2 小时，今天更早活动的未归档线程归入待处理。空状态和 tooltip 明确解释标准。
- 线程标题过滤 Markdown 链接地址，避免任务卡直接显示长 URL 并截断正文。

### 完整审计修复

- 每轮刷新主动清除 Codex / Claude 聚合快照，修复 60 秒缓存与 60 秒定时器叠加造成数据隔轮更新的问题；未变化的 rollout 文件仍复用解析结果。
- rollout 文件级缓存限制为最近 1024 项，避免长期运行时缓存无界增长；SQLite 同时兼容 `.codex/state_5.sqlite` 与 `.codex/sqlite/state_5.sqlite`。
- 所有 Codex SQLite 查询统一使用只读连接与 1 秒超时，避免误建数据库或长时间占用 Codex 正在写入的状态库。
- 全局快捷键注册冲突时恢复上一个已生效组合键，避免设置失败后原快捷键也失效。
- GitHub 自动更新检查通过 Qt Signal 回主线程提示，避免工作线程调用 `QTimer.singleShot` 无事件循环导致通知丢失。
- Windows 通知区图标几何信息不可用时，快速状态面板回退到当前屏幕右下角，不再可能出现在左上角。

### 数据核验与验证

- 本机 SQLite 实际核验：当前共 8 条历史归档线程，但今天归档为 0，因此“完成 0”是当前真实结果；本轮修复保证旧线程今天归档时也能立即进入完成列。
- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：38 项通过。
- Qt offscreen 验证窗口及三张主 Tab 可创建；真实 Windows 窗口验证月份标题完整、项目行图标可见、任务空状态文案正确。更新后执行单实例重启，最终实例数为 1。

## 2026-07-13 · 0.1.01 · Windows 状态栏、设置交互与指标布局精修

### UI 与交互

- 顶部今日/本周/本月/累计卡片删除趋势徽章，未缓存、缓存、输出统一为单行展示，不再形成额外数据块。
- 每日 Token 活动热力图改为横纵双向自适应：月份标题、首行、末行分别与右侧趋势卡的标题区、绘图区和 X 轴基线协调对齐，并充分利用卡片宽高。
- 羊毛进度的 Plus、Pro 100、Pro 200 使用蓝、紫、洋红三档节点与进度段，订阅档位更容易扫视区分。
- 主 Tab、额度窗口、羊毛进度、Token 活动、趋势、项目排行、活动概览、Skill/工具列表和设置页补齐统一 SVG 图标。
- 设置页所有下拉框统一改用 Qt 自绘列表、圆角选中项和一致的 hover/focus 状态，避开 Windows 原生弹层的方框样式。
- 全局快捷键改为点击录制控件，支持直接按下组合键、冲突检测和恢复 `Ctrl+U` 默认值；只有系统注册成功后才会写入配置。

### Windows 与刷新

- Windows 11 通知区新增动态额度环：按当前 Runtime 和已用/剩余口径显示 5h/7d 状态，同时同步为主窗口任务栏图标；tooltip 展示 Runtime、额度和今日 token，单击打开快速状态悬浮面板。
- 遵循 Windows 11 通知区规范，不尝试注入常驻任意文本；用户可在系统“其他系统托盘图标”中将 CodexUU 固定显示。
- 自动刷新固定为 60 秒 `VeryCoarseTimer`，继续使用刷新互斥；前端移除刷新完成状态块，只在刷新按钮 tooltip 中保留最近结果。托盘图标仅在 Runtime、口径或整数百分比变化时重绘。
- 参考上游 codexU 的低开销刷新策略，避免 30 秒全量 session 扫描和空闲托盘重绘回环。

### 版本与验证

- 建立以 `VERSION` 为单一来源、`app/constants.py` 运行时读取并由测试校验的版本机制。历史共 10 轮更新，本轮按十轮进位规则定为 `0.1.01`：`0.0.10 → 0.1.01`，之后每段到 `.10` 后前一位递增、末段回到 `.01`；每轮同步更新 README、AGENTS 和本日志。
- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：33 项通过。
- Qt offscreen 验证真实数据趋势页、设置下拉框、快捷键录制器和托盘状态图标可创建；更新后已执行单实例重启。
- Windows 原生验证 `Ctrl+U` 已被应用独占注册；30 秒空闲采样 CPU 增量为 `0.000s`，未观察到托盘重绘或后台刷新空转。

## 2026-07-13 · 0.0.10 · Windows 产品化设置与视觉系统升级

### 必须修复

- 每日 Token 热力图改为底部基线布局：最后一行与右侧趋势图 X 轴使用相同的 28px 底部留白，不再出现左侧卡片底部额外空白。
- 主导航 `Skill / 工具` 统一命名为 `Skill`；工具调用继续作为 Skill 页面内的关联数据，不再占用导航名称。
- 额度窗口新增“剩余 / 已用”切换并持久化；已用口径顺时针、剩余口径逆时针，百分比和单/双环按真实窗口动态展示。

### 高级感优化

- 数字与英文标题使用 Segoe UI Variable，指标数字增加刷新缓动；羊毛进度加入平滑增长，并提供“减少动态效果”开关。
- 未缓存、缓存、输出统一使用蓝、紫、橙语义色；今日、本周、本月增加环比徽章，累计卡显示官方计价覆盖率。
- 浅色与暗色主背景加入克制的蓝紫环境光，核心卡片采用细微纵向材质、顶部层次边框和轻量阴影。
- 顶部刷新新增可见的刷新中、完成时间和失败反馈；Tab、趋势和数值动效统一响应减少动态效果设置。

### Windows 产品能力

- 新增 Windows `RegisterHotKey` 原生全局快捷键，可在设置中录制并即时生效；冲突或无效组合提供明确状态。
- 新增主窗口始终置顶，以及关闭时隐藏到托盘、最小化或退出程序三种行为。
- 单击托盘图标打开快速状态悬浮窗，同时展示 Codex / Claude Code 今日 Token、5h 与 7d 额度，并提供主窗口和设置入口。
- 更新检查切换到 `CiaoBye/codexUU`，设置页新增 Release 页面入口；发现带 `.msi`、`.exe` 或 Windows `.zip` 的新版本时启用下载按钮。
- 系统设置新增数据源诊断，检查 Codex app-server、SQLite、session 精细事件、Claude transcript 和全局快捷键注册状态。

### 工程与文档

- 新增 `README.md`，完整记录功能、数据口径、安装运行、设置、验证和已知边界；版本统一为 `v1.1.0`。
- 新增 `.gitignore`，发布时排除 Python 缓存、测试缓存、临时截图和 QA artifacts。
- `python -m pytest -q`：31 项通过；`python -m compileall -q app main.py`：通过。
- Qt offscreen 已验证浅色真实数据、热力图基线、额度切换、设置页三类产品设置和托盘快速状态悬浮窗。
- 已通过 `scripts/restart.ps1` 重启，并确认最终仅保留一个项目 `pythonw.exe` 实例。

## 2026-07-13 · 0.0.9 · 热力图布局与项目概览复刻

### UI 修正

- 每日 Token 热力图改为按可用高度动态垂直居中，并将纵向单元格间距从 4px 调整为 7px；月份、星期和热力单元形成居中的完整内容组，不再把大块空白全部留在卡片底部。
- 项目排行由单列表格重构为原版式双栏：左侧显示本周/本月/累计项目用量排行、token、估值、线程、活跃时间和相对进度；右侧显示项目活动概览和最近活跃项目。
- 活动概览新增活跃项目、本期新增、Top1 占比、Top3 占比；所有概览指标与排行严格共用当前统计口径，零用量项目不混入本期排行。
- 两栏卡片保持相同高度，列表各自滚动；浅色与暗色主题补齐排行行、概览指标、徽章和进度条样式。

### 上游核对

- 核对上游 README 和 v1.0.3 Release：统计时区、精细今日 token、刷新防重入、统一 K/M/B 格式化、额度与本机数据来源等关键兼容项已覆盖；Windows 版本仍缺少可配置额度“已用/剩余”口径、减少动态效果选项和托盘快速状态面板等非 macOS 专属体验。

### 验证

- 使用真实本机 8 个项目完成项目页截图：本周仅 2 个正用量项目进入排行，右侧保留 8 个最近项目；左右卡片实测高度均为 406px。
- 使用真实近半年数据完成每日热力图截图，热力内容在左卡垂直居中，底部集中空白已消除。
- `python -m pytest -q`：29 项通过；`python -m compileall -q app main.py`：通过。
- 已通过 `scripts/restart.ps1` 重启，并确认最终仅保留一个项目 `pythonw.exe` 实例。

## 2026-07-13 · 0.0.8 · 动态额度窗口与交互动效

### 必须修复

- 额度环改为按返回数据动态生成：当前本机仅有 7d 窗口时只绘制一条 7d 环，不再显示 5h 空环、`5h --` 或无效重置行；两个窗口、单窗口和全无窗口均有独立布局。
- 重置时间改用所选统计时区的精确时间：5h 显示 `HH:mm`，7d 显示 `MM/DD HH:mm`，悬停提供包含秒和时区的完整时间。删除“已显示当前可用窗口”等状态性开发文案。

### UI 与动效

- 主 Tab 增加 180ms 滑动选中块，内容区增加 160ms 淡入与 10px 水平位移；趋势每日/每周/每月/累计切换增加总计 120ms 的交叉淡化。
- 今日/本周/本月/累计拆分信息改为两行，避免窄卡片中输出 token 被截断。
- 顶部主题、刷新、设置、关闭以及四张指标卡图标全部替换为项目内 SVG，减少 Windows 字体差异造成的符号错位。
- 收敛上半区、下半区和统计条的重复外边框，保留内容卡边界，使视觉层级更接近 codexU 单页仪表盘。

### 验证

- 真实本机额度数据为 `5h=None`、`7d=64%`，界面仅显示 7d 环和 `07/20 09:04` 重置时间；5h 重置控件不可见。
- Qt offscreen 验证主 Tab 滑块最终坐标、内容栈索引和趋势周口径切换均正确；浅色、深色真实数据截图检查通过。
- `python -m pytest -q`：29 项通过；`python -m compileall -q app main.py`：通过。
- 已通过 `scripts/restart.ps1` 重启，最终仅保留一个项目 `pythonw.exe` 实例。

## 2026-07-13 · 0.0.7 · 品牌名、设置控件与趋势布局精修

### UI 修正

- 所有对外可见品牌名统一为 `CodexUU`：主窗口标题、主界面品牌、设置窗口、托盘提示和更新通知同步修改。保留 `.codexU` 配置目录、缓存目录及上游仓库地址，避免现有设置丢失。
- 删除 Logo 下方“本机记录 · 用量与额度”副标题，品牌区收敛为 Logo + `CodexUU` 单行结构。
- 设置页 QComboBox 删除 Windows 原生方形箭头槽，新增统一 SVG 下箭头、圆角、右侧留白、hover/focus 边框和下拉列表样式；浅色与暗色主题分别适配。
- 趋势页每日/每周/每月/累计口径切换移到左侧；“Token 活动 + 当前范围数据”放回左侧卡片标题栏，不再悬在卡片外。
- 左右趋势卡使用相同标题栏结构、内边距和内容伸展比例；真实 Qt 几何检查两侧高度均为 297px。

### 本轮边界

- 用户提出 Tab 动画与整体视觉继续优化，本轮仅输出下一阶段方案，不在方向确认前引入动画代码或额外重构。

### 验证

- Qt offscreen 对设置页下拉框和趋势页双栏完成真实界面截图检查；窗口标题与设置标题均为 `CodexUU`，品牌副标题控件已移除，四个趋势口径按钮起点为左侧 0px。
- `python -m pytest -q`：29 项通过；`python -m compileall -q app main.py`：通过。
- 已通过 `scripts/restart.ps1` 重启，并确认最终仅一个项目 `pythonw.exe` 实例。

## 2026-07-13 · 0.0.6 · 本机口径、四卡与项目过滤修正

### 数据口径

- 明确 Codex App 账号活动页与 codexU 本地统计不是同一数据源：账号活动页可覆盖跨设备、云端任务和更长账号历史；当前应用只读取本机 `~/.codex/sessions`、`archived_sessions` 与 `state_5.sqlite`。主界面副标题改为“本机记录 · 用量与额度”，不再暗示账号云端累计。
- 顶部 token 卡改为今日、本周、本月、累计四张；本周严格按所选统计时区的周一开始计算，保留滚动近 7 天数据仅用于内部兼容，不再用于顶部本周卡。
- Codex 与 Claude Code 的 TokenStats 同步增加自然周拆分和对应价值估算。

### 必须修复

- 修复暗色主题下额度双环中心文字和其他自绘控件误判为浅色主题的问题：ThemeManager 现在同时更新 Qt `QPalette` 与样式表，自绘组件可获得正确的背景亮度。
- 删除没有接入真实账号套餐状态的 PRO 徽章，并删除顶部低价值置顶按钮；保留主题、中/EN、刷新、设置和关闭。
- 项目排行改为只统计仍存在的真实目录：排除已删除项目、日期型临时会话目录、Temp/AppData/`.codex`；创作项目允许没有代码清单，但目录必须存在且非空。
- 本机真实数据过滤后，项目榜由 11 项缩减为 8 项；已删除的旧“品牌内容情报与分析工具”、`fen`、`wo-d`、`ni-s` 等不再出现。已删除项目的历史 token 仍属于本机累计，不从总量中篡改删除。

### 热力图

- 旧实现按“单日最大值线性归一化”生成连续蓝色，峰值过大会压低其他活跃日的色差，且颜色不接近 Codex 账号活动页。
- 新实现按非零活跃日的 25%/50%/75% 分位数划分四档，浅色使用灰底到浅蓝/亮蓝，暗色使用深蓝灰到亮蓝；峰值不再吞掉普通活跃日的层次。
- “Token 活动”标题与当前统计摘要移到图表框架上方，图表内部只保留数据本体。

### 验证

- 真实本机四卡烟测：今日与本周约 59.7M、本月约 223.9M、本机累计约 692.5M；当前日期为周一，因此今日与本周相同属于正确结果，数值会随任务继续变化。
- 深色主题、四卡、热力图、过滤后项目榜均完成 Qt offscreen 截图检查；额度中心文字已使用暗色主题前景色。
- `python -m pytest -q`：29 项通过；`python -m compileall -q app main.py`：通过。
- 已通过 `scripts/restart.ps1` 重启，并确认最终仅一个项目 `pythonw.exe` 实例。

## 2026-07-13 · 0.0.5 · 额度、价格与统计交互修复

### 必须修复

- 定位额度窗口为空的三层原因：Microsoft Store `WindowsApps` 执行别名不能启动 stdio app-server；新版额度结构使用 `primary/secondary + windowDurationMins` 而非 `5h/7d` 键；当前账户响应只提供 7 天窗口。现在优先读取 app-server，失败后回退到最新 session `token_count.rate_limits`，按 300/10080 分钟映射窗口；缺失的 5 小时窗口保持 `--`，不伪造数据。
- API 等效价值改为跟随 session 中实际模型，分别按 OpenAI 官网公开的输入、缓存输入、输出单价聚合。已覆盖 GPT-5.6 Sol/Terra/Luna、GPT-5.5、GPT-5.4、GPT-5/Codex、mini/nano；第三方、内部和无法识别的模型不再套统一价格，界面显示未计价 token 与官方价格覆盖率。
- 项目价值的计价覆盖率按本周、本月、累计独立计算，不再在切换统计口径后沿用累计覆盖率。

### UI 与交互

- 新增 codexU 专属 SVG Logo，并用于窗口、托盘和主界面品牌区；移除主界面 Codex / Claude Code 切换，将数据源移入设置页。
- 顶部操作区按原版逻辑恢复自动/浅色/深色主题分段控件、中文/English 切换、PRO 标识、置顶、刷新、设置和关闭。
- 羊毛进度加入 Plus、Pro 100、Pro 200、满额 `$46.5K` 四个明确节点，使用分段非线性位置保留低额度辨识度。
- 用量趋势新增每日、每周、每月、累计四种切换：每日补齐最近 30 个自然日，周/月使用连续日历区间，累计曲线与 SQLite 全量索引总数对齐；顶部补充累计、峰值、活跃日、最长连续天数。
- 项目排行新增本周、本月、累计分段切换，使用自然周和自然月口径排序。
- 主界面与设置页补齐中英文切换，清理“本地开发”“精细 session”等实现层文案，改为面向用户的状态说明。

### 验证

- 真实本机数据读取：官方额度事件当前返回 7 天剩余约 72%，未返回 5 小时窗口；官方价格计价覆盖约 77.6%，其余模型明确未计价。数据会随当前任务继续变化。
- 使用真实 Codex 数据完成今日任务、四种趋势、三种项目口径、Skill / 工具、中英文和设置页 Qt offscreen 截图验收。
- `python -m pytest -q`：28 项通过，耗时约 0.22 秒；`python -m compileall -q app main.py`：通过。
- 已通过 `scripts/restart.ps1` 重启，并按绝对 `main.py` 路径确认最终仅一个 `pythonw.exe` 实例。

## 2026-07-13 · 0.0.4 · 单页仪表盘与统计口径重构

### 必须修复

- 按 codexU README 与主界面逻辑重构为单页仪表盘：额度、今日/近 7 天/累计 token、本月羊毛进度固定常驻，下半区通过“今日任务 / 用量趋势 / 项目排行 / Skill / 工具”四个 Tab 原地切换。
- 删除左侧导航和独立概览页，避免切换数据时丢失全局用量上下文；Tab 标题栏改为显示当前视图的数据规模。
- 工具统计改为解析本机 session 中明确的 `function_call` / `custom_tool_call` 事件；真实识别到约 5,800 次调用，不再只读取旧字段得到十几次记录。
- 删除单工具美元金额：无法从现有日志把 token 精确归因到每次工具调用，禁止继续按调用次数硬摊累计价值。
- Skill 仅从实际读取 `SKILL.md` 或显式 Skill 字段的调用记录统计，不把已安装 Skill 或普通文本提及计入使用次数。
- 累计 token 优先显示 `state_5.sqlite.threads.tokens_used` 汇总，输入/缓存/输出仍使用精细 session `token_count`，并通过悬停说明两种来源。
- 修复羊毛进度错误使用累计 token：现在只聚合所选统计时区下的本月精细 token；累计卡继续展示累计 API 等效价值。

### UI 与交互

- 上半区改为左侧蓝紫额度双环、右侧三张 token 卡及横向本月价值进度，信息层级与 codexU 截图一致。
- 下半区使用分段按钮切换四个数据面板；工具与 Skill 列表改成等宽双栏、独立滚动和紧凑排行卡。
- 清理任务区重复数量和横向滚动条；浅色/深色主题补齐单页摘要、Tab 容器、排行条目的状态样式。
- 额度接口不可用时保留诚实的 `--` 双环和说明，不伪造百分比。

### 上游核对与验证

- 核对上游 README 的单页下方仪表盘、SQLite 累计总量、精细 `token_count`、工具/Skill 加载数据源及本月羊毛公式。
- 核对 v1.0.3 Release：今日 token 优先精细事件、统计时区统一、刷新防重复仍保持现有实现。
- 真实本机数据烟测：今日约 39.2M、近 7 天约 112.2M、SQLite 累计约 671.9M、本月约 203.4M；数值会随当前任务继续增长。
- `python -m pytest -q`：22 项通过；`python -m compileall -q app`：通过。
- 1180×800 Qt offscreen 对今日任务、趋势、项目、Skill / 工具四个 Tab 完成截图验收。

## 2026-07-13 · 0.0.3 · 真实数据与 UI 纠偏

### 必须修复

- 修复概览今日/近 7 天错误显示为 0：现在与精细 session 趋势使用同一统计口径。
- 修复新版 `state_5.sqlite` 表结构不兼容导致今日任务始终为空的问题。
- 使用 SQLite 中 `rollout_path → cwd` 映射项目，项目排行不再全部归入 `default`。
- Microsoft Store 版 Codex 的执行别名无法作为 stdio app-server 时立即回退，避免每次刷新卡住约 8 秒；额度明确显示不可用，不再伪装成 0。
- session 解析增加按文件修改时间和大小缓存，首次扫描后刷新从约 8 秒降至约 1 秒。

### UI 纠偏

- 概览改为紧凑额度横条、三项 token 卡、非线性价值进度、本机数据状态和最近活跃项目，消除整屏拉伸空卡。
- 今日任务列改成有边界的状态面板，空状态和任务卡保持稳定结构。
- 项目列表移除灰色空白底，补充真实项目名、来源、最近活跃、token、估算价值和线程数。
- 设置窗口缩小到内容匹配尺寸，并为分组卡片、输入框和浅/深主题补齐样式。
- 修复重启脚本未命中相对路径实例的问题；清理 3 个残留旧实例后，改用绝对 `main.py` 路径启动并确认只保留 1 个进程。
- 新增 `scripts/restart.ps1`，以后统一执行关闭旧实例、绝对路径启动和单实例校验。

### 真实数据验收

- 概览已读取到今日、近 7 天和累计精细 token；数值与趋势聚合一致。
- 近 7 天折线改为连续自然日矩阵，缺失日期明确按 0 展示，不再误用“最近 7 个活跃日”。
- 今日任务读取到本日线程，项目排行按工作目录拆分为多个真实项目。
- 回归测试增加概览与趋势口径一致、Store 执行别名快速回退检查。

## 2026-07-13 · 0.0.2 · Windows UI 重构

### UI

- 将主窗口改为 Windows 桌面工具结构：顶部品牌与 Runtime 操作栏、左侧页面导航、右侧内容区。
- 重做概览页的额度、今日/近 7 天/累计 token、API 等效价值卡片，减少大面积空白和重复装饰。
- 重做设置窗口为分组卡片式布局，统一浅色/深色主题，清理乱码和不可读的硬编码深色样式。
- 任务卡补充任务标题；项目排行补充来源、最近活跃时间和全部/近 7 天口径；工具列表补充分类。

### 功能

- 趋势页改为固定周矩阵热力图和近 7 天折线图，支持 hover 查看日期、token 拆分和周期摘要。
- 更新偏好（自动检查、Beta 接收）持久化，并在启动后异步检查 GitHub Release。
- 项目统计增加精细 session / Claude transcript 来源标识。
- 工具统计增加命令执行、文件操作、版本控制、网络访问等分类，并按调用占比展示 API 等效价值估算。
- 今日任务按统计时区过滤当天线程，保留定时 automation 和无时间记录任务。

### 验证

- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：18 项通过。
- Qt offscreen 主窗口与设置窗口烟测：通过。
- 已完成程序重启。

## 2026-07-13 · 0.0.1 · 初始 Windows 跟进

### 非 macOS 功能跟进

- 统一 Codex / Claude Code 的 runtime 数据筛选，避免趋势、项目、任务、工具和 Skill 数据串台。
- 修复 Codex `token_count` 累计快照重复计数，补充 SQLite 回退和 JSON-RPC app-server quota 读取。
- 增加系统、UTC、固定 IANA 三种统计时区，并让 Claude、Codex、趋势、任务和 SQLite 统计共用该口径。
- 补齐 Claude transcript 的缓存创建/读取 token 统计。
- 趋势页增加近 7 天总量、日均和前 7 天对比；项目排行增加“全部 / 近 7 天”。
- 更新检查增加稳定版/Beta 筛选、ETag、24 小时缓存、负缓存和 Windows 缓存路径。
- 设置页接入异步更新检查；Windows 固定 IANA 时区补充 `tzdata` 依赖。

### 验证

- `python -m compileall -q app main.py`：通过。
- `PYTHONPATH=. python -m pytest -q`：18 项通过。
- Qt offscreen smoke test：通过。
- 更新完成后已重启程序；若本次执行前程序未运行，则已启动程序进行验证。
